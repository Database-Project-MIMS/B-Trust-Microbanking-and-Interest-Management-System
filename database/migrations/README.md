# database/migrations

Append-only, numbered migrations. See `../README.md` for naming, execution order and reserved number blocks.

Current: `0000_p00_shared_foundation.sql` (shared foundation, Phase 0).
