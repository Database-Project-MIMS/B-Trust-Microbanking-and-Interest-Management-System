# lib/validation/

Server-side input schemas. Every route handler validates its input here before
calling a service. Client-side validation is for usability only and is never trusted.

_Empty until Phase 1._
